<div class="container">
  <div class="row">
    <div class="col">
    <%= render 'form', book: @book %>
    </div>
  </div>
  <div class="row">
    <div class="col book-title">
      <% @books.each do |book| %>
        <%= book.title %>
      <% end %>
    </div>
  </div>
</div>

<%= link_to 'login' %>