<%= form_with model: book, remote: true, local: false, method: :post do |f| %>
  <%= f.text_field :title, placeholder: "本のタイトル" %>
  <%= f.text_area :body, placeholder: "本の内容" %>
  <%= f.submit "投稿", class: 'btn btn-sm btn-success' %>
<% end %>